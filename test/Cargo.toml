[package]
name = "test"
version = "0.1.0"
edition = "2018"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
cobul = { path = "..", features = ["custom", "fa", "forms"] }
wasm-bindgen = "0.2"
derive_more = "0.99.16"
strum = { version = "0.21", features = ["derive"] }
yew = "0.19.3"
validator = { version = "0.14", features = ["derive"] }